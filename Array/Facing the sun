    def countBuildings(self, height):
        lnt = len (height)
        max_height = height[0]
        count = 1
        for i in range(1,lnt):
            if max_height < height[i]:
                count = count +1
                max_height = height[i]
                
        return count
